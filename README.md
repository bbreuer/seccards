# SecCards 🛡️

CompTIA Security+ (SY0-701) flashcard web app. 381 cards across all 17 chapters.

Open `index.html`, or visit the GitHub Pages site. Edit decks in `decks/chN.json` then run `python3 build_data.py` to rebuild `flashcards-data.js`.
